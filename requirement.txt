vllm==0.11.0
# vllm 0.11 declares transformers>=4.55.2 with no upper bound; transformers 5.x
# removed APIs vllm relies on (e.g. all_special_tokens_extended). Pin to 4.x.
transformers==4.57.1
torch>=2.0
numpy>=1.24
blake3
zstandard

# science hallucination analyzer (science_hallucination_analyzer.py)
scikit-learn
